apiVersion: v1
kind: Service
metadata:
  name: verana-io-website
  namespace: web
  labels:
    app.kubernetes.io/name: verana-io-website
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: verana-io-website
